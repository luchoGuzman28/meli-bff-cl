'use strict';

const profile = require(`./profile`);
const shipment = require(`./shipment`);
const level = require(`./level`);
const payment = require(`./payment`);

module.exports = { profile, shipment, level, payment };
